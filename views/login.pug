extends layout

block content
  h1= title
  p Welcome to #{title}
    form(method="post")
        p
            label Login
                input(type="text" name='login')
        p
            label Password
                input(type="password" name='password')
        p
            label
                input(type="submit" value = 'LOG IN')
            